$(document).ready(function() {
    // Remove periods from a.m. and p.m.
    // $('i.datetime').each(function(){
    //     $(this).text($(this).text().replace(/\./g, ''));
    //     $(this).removeClass('hidden');
    // });

    // Fakecrop
    // $('img').fakecrop();

});
